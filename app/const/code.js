/**
 * @file 存放执行代码相关的常量
 */

'use strict';

// 代码模板的头部引入包相关代码
exports.CODE_TEMPLATE_HEADER = `
  const _ = require('lodash');
  const lodash = require('lodash');
  const moment = require('moment');
  const dayjs = require('dayjs');
  const uuid = require('uuid');
  const ramda = require('ramda');
`;
